# Page snapshots

Saved "View Page Source" HTML from representative pages, so Claude can see the site's real structure and write precise selectors.

To add one: open the page → right-click → View Page Source → copy all → give it to Claude to save here.

Planned:
- `form-page.html` — a representative form page
- `info-page.html` — a representative info page
